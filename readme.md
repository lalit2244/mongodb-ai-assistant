# 💎 Invock AI Analytics

> Natural language → MongoDB queries → Beautiful charts, tables & business insights

An AI-powered analytics dashboard for **Invock ERP** (jewellery business) built with LangChain, Groq LLaMA-3.3, MongoDB, and Streamlit.



---

## 🚀 Features

- **Natural Language Queries** — Ask questions like "Show total sales last month" and get real MongoDB data
- **Auto Chart Selection** — Bar, Donut, Line charts with smart axis detection
- **Live MongoDB Stats** — Real-time collection counts in sidebar
- **₹ INR Formatting** — Revenue shown in Crore/Lakh format
- **CFO-style AI Analysis** — Business insights generated by LLaMA-3.3-70b
- **Dark Premium UI** — Professional dashboard aesthetic

## 📸 Demo

Ask questions like:
| Question | Result |
|---|---|
| "Which product sold the most?" | Bar chart of top items by qty |
| "Top 10 customers by revenue" | Donut + Bar with ₹ amounts |
| "Monthly sales trend last 12 months" | Line chart with area fill |
| "Show unpaid invoices" | Filtered table with status |
| "Total revenue this year" | Big KPI metric card |
| "Sales vs purchases comparison" | Side-by-side bar chart |

---

## 🛠️ Tech Stack

| Layer | Technology |
|---|---|
| UI | Streamlit 1.40 |
| AI/LLM | Groq API — LLaMA-3.3-70b-versatile |
| AI Framework | LangChain 0.3 |
| Database | MongoDB Atlas (dev-cluster) |
| Charts | Plotly Express + Graph Objects |
| Data | Pandas |
| Driver | PyMongo 4.x |

---

## ⚡ Quick Start

### 1. Clone the repo
```bash
git clone https://github.com/YOUR_USERNAME/invock-ai-analytics.git
cd invock-ai-analytics
```

### 2. Create conda environment
```bash
conda create -n mongo-ai python=3.11 -y
conda activate mongo-ai
```

### 3. Install dependencies
```bash
pip install -r requirements.txt
```

### 4. Set up environment variables
```bash
cp .env.example .env
# Edit .env and add your GROQ_API_KEY
```

Get a free Groq API key at: https://console.groq.com

### 5. Run the app
```bash
python -m streamlit run app.py
```

Open http://localhost:8501 in your browser.

---

## 📁 Project Structure

```
invock-ai-analytics/
├── app.py              # Streamlit UI + chart rendering
├── agent.py            # AI agent — LangChain + MongoDB logic
├── requirements.txt    # Python dependencies
├── .env.example        # Environment variables template
├── .env                # Your secrets (git-ignored)
├── .gitignore
└── README.md
```

---

## 🔧 Configuration

Create a `.env` file:
```env
GROQ_API_KEY=gsk_your_key_here
MONGODB_URI=mongodb+srv://mcpaccess:mcpaccess@dev6.4hksq.mongodb.net/dev-cluster
```

---

## ☁️ Deploy on Streamlit Cloud

1. Push to GitHub (see deployment steps below)
2. Go to https://share.streamlit.io
3. Click **New app** → select your repo
4. Set **Main file path**: `app.py`
5. Add secrets in **Advanced settings**:
   ```toml
   GROQ_API_KEY = "gsk_your_key_here"
   MONGODB_URI = "mongodb+srv://..."
   ```
6. Click **Deploy**

---

## 🏗️ How It Works

```
User Question
     ↓
LangChain + LLaMA-3.3-70b (Groq)
     ↓ generates MongoDB pipeline
PyMongo executes query on Atlas
     ↓
Pandas DataFrame
     ↓
Plotly Charts (Bar / Donut / Line)
     ↓
Streamlit renders result + AI analysis
```

---

## 📊 Database Collections

| Collection | Docs | Purpose |
|---|---|---|
| Voucher | 1.3M | All sales/purchase transactions |
| ItemQuantityTracker | 2.1M | Item-level sales analytics |
| Item | 450K | Product catalog |
| Business | 45K | Customers & suppliers |
| ItemSummary | 147K | Voucher line summaries |
| Account | 51K | Chart of accounts |

---

## 👨‍💻 Author

Built as a portfolio project demonstrating:
- AI-powered natural language to database query translation
- Real-time MongoDB aggregation pipeline generation  
- Production-grade Streamlit dashboard development
- LangChain + Groq LLM integration

---

## 📄 License

MIT License — free to use and modify.
